<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">

        <div class="w-full flex justify-end">

            <a href="/logout" class="px-3 py-2 bg-red-500 rounded-md inline-block mb-3">Logout</a>

        </div>

        <div class="flex flex-col justify-center items-center">

            <div class="flex gap-3 place-items-center w-[650px]">

                <div class="w-10 h-10 rounded-full overflow-hidden">
                    <img class="w-full h-full object-cover " src="/images/uploads/<%= user.profilepic %>" alt="Avatar" onerror="this.onerror=null; this.src='/images/default-avatar.jpg';">
                </div>

                <h1 class="text-3xl font-medium">Welcome, <%= user.name  %>👋</h1>

                <a href="/profile/upload" class="px-3 py-2 ml-auto text-sm mt-3 block bg-red-500 rounded-md ">Change Profile Pic</a>

            </div>

            <h5 class="mb-3 mt-5">You can create a new Post!</h5>

            <div class="w-[650px]">
                <form action="/post" method="post"> 

                    <textarea class="w-full p-3 block resize-none bg-transparent border-2 border-zinc-700 rounded-md outline-none" rows="3" placeholder="Write your post here..." name="content" ></textarea>

                    <input class="px-6 py-2 text-sm mt-3 block bg-blue-500 rounded-md " type="submit" value="Create new post">

                </form>
            </div>
        </div>

        <div class="posts mt-16 flex flex-col ">

            <h3 class="text-xl text-center text-zinc-400 mb-3">Your All Posts!</h3>

            <div class="postcontainer flex flex-col justify-center items-center">

                <% user.posts.reverse().forEach(function(post){ %>

                    <div class="post px-5 py-2 mb-2 w-[650px] border-[1px] border-zinc-700 bg-zinc-800 rounded-md">

                        <h4 class="text-blue-500 mb-1 font-medium"> @<%= user.username  %></h4>
                        <p class="text-sm tracking-tight"><%= post.content %></p>

                        <small class="inline-block mt-3"><%= post.likes.length %> likes</small>
                        <div class="buttons flex gap-8 mt-1">
                            <a href="/like/<%= post._id %>" class="text-blue-500 text-sm"><%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %></a>
                            <a href="/edit/<%= post._id %>" class="text-zinc-400 text-sm">Edit</a>
                            <a href="/delete/<%= post._id %>" class="text-red-500 text-sm ml-auto">Delete</a>
                        </div>

                    </div>
                
                <% }) %>
                
                
                
            </div>
        </div>

    </div>
</body>
</html>